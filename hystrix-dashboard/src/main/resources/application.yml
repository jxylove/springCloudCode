server:
  port: 8501
eureka:
  client:
    service-url:
     defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

spring:
  application:
    name: HYSTRIX-DASHBOARD

management:
  endpoints:
    web:
      exposure:
        include: "*"
